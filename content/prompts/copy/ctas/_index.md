# Copy CTAs — index

Reusable call-to-action lines, grouped by funnel stage and platform. Promote winners to `prompts/canonical/` once metric-proven.

---

## Categories currently catalogued

| Category | File | Notes |
|---|---|---|
| (none yet) | — | Add as the library grows |

---

## Promotion rule

Move a CTA to `prompts/canonical/` when:

1. It has shipped in a real client campaign.
2. It performed against a defined metric (CTR, CVR, lead quality).
3. Attribution is recorded (client, campaign, period, metric).
