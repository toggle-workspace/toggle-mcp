# Client folder template

Copy this folder for every new client:

```bash
cp -r clients/_TEMPLATE clients/<slug>
```

Then fill `CLIENT.md` and `style-pack.md`. Create subfolders only when you start work that requires them — don't pre-create empty subfolders.

See `clients/CLAUDE.md` for the rules of this zone.
